# 沼底バリュー商会ブログ 引き継ぎ書

> 新しいスレッド（セッション）でこのファイルを最初に読めば、作業を継続できます。
> 本番サイト: https://numasoko-value.com/
> 最終更新: 2026-09-08（記事111本・下書き5本。**scripts/precheck.py と scripts/price.py を必ず使うこと**）

---

## 0. 最重要・絶対に間違えないこと

1. **デプロイは必ず `--branch=master`**（本番ブランチは `master`。`main` は Preview で本番に出ない）
   ```bash
   cd /c/Users/fuzzy/Documents/my-investment-blog
   rm -rf dist && npm run build
   CLOUDFLARE_API_TOKEN=$(cat .cf_token) npx wrangler pages deploy dist --project-name=investment-blog --branch=master
   ```
2. **CJK太字バグ**：閉じ `**` が「記号（`%` `％` `+` `▲` `≈` `"` `」` `）`）の直後」かつ「直後がかな漢字」だと太字が壊れ、画面に `**` が出る。開き `**` が「かな漢字の直後」かつ「直後が記号」でも壊れる。
   - **対処**：語尾・助詞を太字内に含めて閉じを文字で終える（例 `**7%**だ` → `**7%だ**`）。**ASCII `+` 始まり**（例 `**+4.5万件**`）や `A+` を含む太字（例 `**…A+**を`）も壊れるので、その手の箇所は `<strong>…</strong>` を使う。
   - **必ずビルド後に検査**：`grep -rho '[*][*]' dist/blog/*/index.html | wc -l` が **0** であること。
3. **YAML破損**：frontmatter の `title`/`description`/`cardDesc` は**ダブルクォート `"…"` の文字列**。中で ASCII の `"` を使うと壊れる → **和文引用符 `『』`「」** を使う。（※本文＝マークダウン中は ASCII `"` OK）
4. **pubDate は必ず時刻＋JSTオフセット付き**（例 `"2026-07-07T10:00:00+09:00"`）。日付だけだとUTC午前0時扱いで表示が1日ズレる。同日複数公開は時刻で並び順を確定。
5. **frontmatter の `draft:` は1つだけ**。保留↔公開を切り替えるときに `draft` 行を二重に足すと `js-yaml: duplicated mapping key` でビルドが落ちる。
6. デプロイ後の本番検証は**エッジ伝播ラグ**あり（直後は grep 0 でも数秒〜十数秒で反映）。`?v=$(date +%s)` を付けて数回リトライ。**存在しないパスは404でなく200で汎用ページ**（title「沼底バリュー商会」）を返すので、公開/非公開の判定は**記事本文が入っているか**で見る。CSS/レイアウト変更もエッジラグで旧版が返ることがある（例：ボタン幅が揃わない→伝播後に解消）。ローカル `dist/_astro/*.css` に新ルールが入っていれば反映済み＝待てばよい。
7. **記事を全面Write（新規/作り直し）したら `draft:` 行の有無を必ず確認**（`grep -n '^draft:'）。在庫のつもりが draft 抜けで公開扱いになるヒヤリが発生済み。一時コピー検証で `[ -d dist/blog/<slug> ]` が"在庫除外OK"になることも確認する。
8. **保有データの xlsx は Windows で LibreOffice 再計算（recalc.py）が動かない**（`socket.AF_UNIX` 無し）→ 数式でなく**計算済みの値**を入れて生成する。**Windows PythonはGit Bashパス（`/c/...`）を解釈できない**→スクリプトのファイルパスは Windows形式（`C:/...`）で書く。

---

## 1. プロジェクト基本

- 場所: `C:\Users\fuzzy\Documents\my-investment-blog`
- 技術: Astro（SSG）。記事は `src/content/blog/*.md`
- デプロイ: Cloudflare Pages（`wrangler`）。トークンは `.cf_token`（リポジトリ内）
- 本番ドメイン: numasoko-value.com（= Pages の master ブランチ）
- **下書き機能あり**：frontmatter に `draft: true` を付けると、一覧・ホーム・/map・RSS・ページ生成すべてから除外される（スキーマ・全箇所実装済み）。ドリップ配信の要（→ §10）。

---

## 2. 新規記事の追加手順（チェックリスト）

1. `src/content/blog/<slug>.md` を作成（下のテンプレ参照）
2. **コラムの場合**: `src/data/columnAuthors.ts` に `'<slug>': C.<author>,` を追加（銘柄記事は不要）
3. **テーマ分類**: `src/data/contentMap.ts` の該当テーマ `slugs:` に `<slug>` を追加（draft中に足してもOK＝/mapは公開記事だけ表示）
4. **銘柄記事の場合**:
   - 完全版HTMLを `public/analysis/<slug>.html` に設置（§7のモバイルブースト注入）
   - frontmatter に `stockCard`（**`rating` 必須**＝ウォッチリストの元データ）と `noteUrl`（= 完全版HTMLのURL）
   - **ホームのウォッチリストは stockCard.rating から自動生成**（2026-07-08改修済み・手動追加は不要）。draftは自動除外→公開した瞬間に自動で載る。表示名を短くしたい場合のみ `shortName` を指定（例：広済堂HD）
   - ヒーローの「📊 完全版分析資料」リンクは **noteUrl から自動導出**（登録作業不要）
5. ビルド→`**` 0 検査→`--branch=master` デプロイ→本番curl検証

### コラム frontmatter テンプレ
```yaml
---
title: "SEOタイトル｜サブ見出し【シリーズ名①】"
description: "120〜160字程度。和文引用符のみ。"
cardDesc: "<span class='card-highlight'>キャッチ一文。</span>補足。<span class='card-note'>担当キャラ・シリーズ表記。</span>"
pubDate: "2026-07-07T10:00:00+09:00"   # 時刻+JSTオフセット必須・未来日付にしない
related: ["slug1", "slug2", "slug3", "slug4"]
draft: false
---
```

### 銘柄 frontmatter テンプレ（stockCard）
```yaml
draft: false
stockCard:
  rating: "A-"        # 必須。A++/A+/A/A-/B++/B+/B（ASCIIのハイフン）。ウォッチリスト自動生成の元データ
  # shortName: "広済堂HD"   # 任意。ウォッチリスト表示名を短縮したい場合のみ
  ticker: "1450"
  companyName: "TANAKEN"
  currentPrice: 1385   # 記事ヒーローに「pubDate時点」と自動表示される（基準日＝pubDate）
  targetPrice: 2072
  upside: "+25〜+75%"
  downside: "▲13%"
  modifiedPBR: "1.29倍"
  category: "解体専業唯一・資材高耐性・親子上場割安"
noteUrl: "https://numasoko-value.com/analysis/tanaken-1450.html"
```
※ `rating` はホームのウォッチリストに直結（自動生成・ランク順・同ランク内は新しい順）。評価を改定したらここを書き換えてデプロイするだけ。

---

## 3. キャラクター（発信元 columnAuthors の C.キー）

| キー | 絵文字 | 名前 | 役割 |
|---|---|---|---|
| numata | 🧑‍💼 | 沼田課長 | 統合・哲学（リーダー） |
| morita | 🦞 | 守田退三 | リスク・安全余裕・懐疑（ザリガニ） |
| kawachi | 🦦 | 河内拾造 | 割安発掘 |
| hanaoka | 🐝 | 花岡利次郎 | 配当・株主還元（ミツバチ） |
| hotta | 🦫 | 堀田独占 | 競合優位性・モート |
| yomi | 🦉 | 夜見賢三 | 財務分析 |
| machibuse | 🐊 | 待伏静江 | TOB・イベントドリブン |
| nomura | 🧓 | 野村沼之助 | 創業者・レバレッジ（レジェンド枠でふらり登場） |
| hinata | 🦎 | 日向昇（見習い） | 問題提起・素朴な疑問役（「将来は龍」） |

その他レジェンド/ライバル（本文絵文字のみ）：🦍テック番長、🦅ドル建て鷹、🐒レバナス小猿、🦩優田（優待）、🧘オルカン教祖。※優田は実質準レギュラー化（優待担当としてPR節を持つ）。

### 掛け合い設計ルール（2026-07-21・ユーザーフィードバックで明文化）
**「中身は良いが掛け合いが少なくて寂しい」を防ぐ。全記事に最低1つ、できれば2箇所の掛け合いを入れる。**
- **型＝ボケ→ツッコミ→回収**：①ライバル（or 日向）が読者が思う"痛いところ"を突く（ボケ） ②担当キャラが**数字と構造**で切り返す（ツッコミ） ③最後に「その茶々こそ確認すべき論点だった」と回収し、**茶々をチェックリストに変換**する（納得感）。論破で終わらせず、**相手の指摘の正しい部分を認めて規律に変換**する（例：教祖の分散論→準主力3〜5%上限はその実装、と認める）。
- **ライバルの人選はテーマで**：🦍テック番長＝技術・AI代替・グロース夢／🐒レバナス小猿＝待てない・短期・レバレッジ／🧘オルカン教祖＝分散・効率市場・個別株否定／🦅ドル建て鷹＝為替・海外比較。
- **回収の定番セリフ役＝日向**：「ライバルのツッコミに全部答えられる銘柄だけが残ってる」系の気づき→沼田が「茶々は無料の査読」で締める。
- **ライバルは商会の"内輪の意思決定"（PF現況の売買判断など）には混ぜない**（現況①でテック番長を出して不適とされた経緯）。銘柄紹介・コラムへの「乱入」はOK。
- 実装例：`towns-197a`（乱入初出）／`construction-selloff-opportunity`・`healthcare-selloff-opportunity`（2026-07-21に乱入節を追記）。

---

## 4. 本文の色強調（global.css に定義済み）

```
<span class="t-red">…</span>    赤   警告・リスク・危険サイン
<span class="t-green">…</span>  緑   好材料・お宝・結論ポジティブ
<span class="t-amber">…</span>  琥珀 注意・条件付き
<span class="t-blue">…</span>   青   中立の重要ポイント
```
方針：**やりすぎない**。要所のみ。締め（💬ブロック）に1フレーズ＋本文数か所が目安。

---

## 5. 記事・シリーズの現状（2026-09-08 更新）

**記事111本（公開106／下書き5）／完全版資料50本／related 平均4.26本・被リンク0本ゼロ**

> 被リンク0本の判定は「related から参照される回数」で数える。配当④・アクティビスト③の
> 2本は related の被参照が0だが、`series.ts` のシリーズナビが全話リンクを張るため孤立ではない。

### シリーズは `src/data/series.ts` で一元管理（2026-09-08 新設）

**記事本文に前後リンクを手書きしない。** `series.ts` に登録すれば `SeriesNav.astro` が
記事下部に全話ナビを自動生成する（現在地ハイライト・下書きは本番で自動非表示）。

| id | シリーズ名 | 話数 | 更新頻度 |
|---|---|---:|---|
| `weekly` | 週次運用記録 | 1 | 毎週 |
| `reverse-value` | 逆バリュー投資（日向が企画者） | 2 | 不定期 |
| `census` | シケモク圏の人口調査 | 1 | **毎月** |
| `scorecard` | 紹介銘柄の通信簿 | 1 | **四半期（次12月）** |
| `dividend` | 配当シリーズ | 4 | 完結 |
| `holdings-status` | 紹介銘柄の現況シリーズ | 5 | 完結 |
| `activist` | アクティビスト先回りシリーズ | 4 | 完結 |

**新しい回を書いたら `slugs` と `labels` に1行ずつ足すだけ。** 既存の全話に自動で相互リンクが入る。
`precheck.py` が接頭辞（`weekly-` `reverse-value-` `cheap-stock-census-` ほか）を監視していて、
登録し忘れるとエラーで止まる。

### related は最大6本表示（2026-09-08 に 4→6 へ拡張）

`src/layouts/BlogPost.astro` の `relatedPicked.slice(0, 6)`。孤立記事を作らないこと。

### 定点観測シリーズの回し方（重要）

- **人口調査（毎月）**：ユニバース＝保有銘柄 ∪ 紹介銘柄。`public/holdings.html` の
  埋め込みJSONから PBR/利回り/PER/時価総額を読む。**ダッシュボード更新の直後に回すのが正しい**
  （基準日が古いと precheck が警告を出す）。条件は PBR0.5倍以下 × 配当2.5%以上、
  ＋PER10倍以下を「三重丸」。**絶対水準ではなく前月比の差分を見る指標**。
  副産物として「条件を通ったのにまだ記事にしていない銘柄」が出るので、そのままネタ帳になる。
- **通信簿（四半期）**：紹介銘柄の stockCard.currentPrice と直近終値を全件突き合わせ、
  日経平均を各保有期間で加重して超過リターンを出す。7日未満は判定前として除外。

---

## 5-A. スクリプト（2026-09-08 新設・必ず使う）

### `py scripts/precheck.py` — 公開前チェック（**ビルド前に必ず通す**）

```bash
py scripts/precheck.py                    # 全記事
py scripts/precheck.py <slug> <slug>      # 個別
py scripts/precheck.py --drafts           # 下書きだけ
py scripts/precheck.py --changed          # git 変更分だけ
```

過去に実際やらかした9項目を検査する。エラーがあれば終了コード1。

1. **span / strong のタグ交差・未閉鎖** … `<span>` を `</strong>` で閉じる事故が頻発。**ほぼ毎回出る**
2. 日本語以外の文字の混入（ヘブライ文字 `סּ`・キリル文字 `это` の実績あり。ギリシャ文字αβは通す）
3. `**` の個数が奇数の行
4. Markdown 表の列数不一致
5. related のスラッグ切れ・自己参照・重複
6. シリーズ記事なのに `series.ts` に未登録
7. frontmatter の必須キー欠落
8. 本文の「YYYY年M月D日終値」と pubDate の乖離（4日以上で警告）
9. `stockCard.currentPrice` と本文の終値の食い違い

**タグ交差が出たときの直し方**（構造的に修復するワンライナー）は §9 参照。

### `py scripts/price.py <code>...` — 確定終値を取る（**株価を書く前に必ず**）

```bash
py scripts/price.py 4040 5408 --days 5
```

場中値を終値と取り違える事故を防ぐためのもの。銘柄ページの `<time>終値</time>` が指す日付を
正とし、時系列の最上段がそれより新しければ「場中の行」として捨てる。
PER・PBR・利回り・信用倍率・時価総額・発行済株式数も返す。

---

## 5-B. 株探スクレイピングの罠（実際に踏んだもの）

新しいスクレイピングを書くときは、以下を必ず疑うこと。**3つとも実際に誤った記事を書きかけた。**

1. **「終値」ラベルの `<time>` は複数ある**
   前日終値と当日終値が並ぶ日があり、**先頭を取ると1営業日古い日付を掴む**。必ず `max()` を取る。
   （`price.py` の `confirmed_date()` で対処済み）
2. **ランキング表は空セルを含む13列**
   `[x for x in cells if x]` のように空セルを詰めると**列がずれる**。
   1年前比をPERとして読み、「東京エレクトロンの時価総額741億円」という出力になった。
   → セルを詰めず、列位置で読む。正しい並びは
   `0:コード 1:銘柄名 2:市場 5:株価 7:1年前終値 9:騰落率 10:PER 11:PBR 12:利回り`
3. **兆円規模の時価総額は「6兆1,535億円」と分かれる**
   最初の `億円` だけ拾うと**兆を落として1,535億円になる**。
   （`price.py` の `metrics()` で対処済み）

さらに **PERが「－」＝赤字ではない**。会社が今期予想を開示していないだけの場合がある
（キオクシア・東エレク・SUMCO が該当）。**必ず個別ページの業績欄で赤字かどうかを確認する。**

### 信用区分（空売り可否）の判定

銘柄ページに `|貸借|` `|信用|` `|現物|` のラベルがある。

- **貸借** … 買建・売建の両方 → **空売り可**
- **信用** … 買建のみ → 空売り不可
- **現物** … 制度信用の対象外 → 空売り不可

新規指定の一覧は **JPX「制度信用・貸借選定銘柄一覧」**
`https://www.jpx.co.jp/listing/others/margin/index.html`
（HTML内3つ目のテーブルが「貸借選定銘柄」。選定日つき。全銘柄の xlsx も同ページにある）

---

## 6. シリーズ記事の連結ルール（**§5 の series.ts に移行済み**）

> ⚠️ 以下は series.ts 導入（2026-09-08）より前の手運用。**前後リンクの手書きはもう不要**。
> 新しいシリーズは §5 のとおり series.ts に登録すること。タイトル記法などは今も有効。

- タイトル末尾に `【シリーズ名①】` を付ける
- cardDesc の `card-note` にシリーズ表記
- 記事末尾に**前後リンク**（`*前回：[…](/blog/…/)（シリーズ名②）*`）
- 新刊を出したら、**前巻の「次回予告/続きリンク」も実リンクに更新**
- 銘柄紹介の連番は破綻済み → **新規銘柄に番号は付けない**運用
- **コラム⇄関連銘柄記事は📎ボックスで強力に相互リンク**する（例：隠れ国策コラム⇄368A/7061）。related配列も双方向登録。

---

## 7. 完全版HTML（銘柄の noteUrl 先）の作り方

`public/analysis/<slug>.html` に置く。元HTMLの `</head>` 直前に**モバイル可読性ブースト**を注入する（既存 `keihan-hd-9045.html` からコピー）。実際に使ったコマンド：
```bash
SCR=/tmp
# ① 既存からboost styleブロックを抽出
awk '/<style id="mobile-readability-boost">/{f=1} f{print} /<\/style>/{if(f)exit}' \
  public/analysis/keihan-hd-9045.html > "$SCR/boost.html"
# ② 元HTMLの </head> 直前に注入して保存
awk 'FNR==NR{boost=boost $0 ORS; next} /<\/head>/{printf "%s", boost} {print}' \
  "$SCR/boost.html" "/c/Users/fuzzy/Downloads/元ファイル.html" > public/analysis/<slug>.html
```
- 記事側は `noteUrl` と本文末 `<details>📊 詳細分析レポート（完全版）` の2か所で導線。noteUrl があると本文下部に大きなCTAが自動表示。
- **CTAラベルは `完全版レポート（無料公開）`**（旧「note 有料記事」から変更済み・`BlogPost.astro`）。
- `.html` へのアクセスは **308→200 の正規化リダイレクト**（拡張子なしへ）＝正常挙動。`curl -L` で追う。

---

## 8. ホームの「📋 紹介銘柄」ウォッチリスト（2026-07-08 自動生成化）

**`stockCard.rating` から自動生成**（`src/pages/index.astro` の `watchlist` 定数）。手動追加は廃止。
- 並び：**A++→A+→A→A−→B++→B+→B**、同ランク内は pubDate 新しい順
- **draft記事は自動除外**＝在庫を公開（draft外し）した瞬間に自動で載る。死リンク・追加漏れは構造的に起きない
- 表示名は `shortName ?? companyName`（広済堂のみ shortName="広済堂HD" 使用中）
- バッジ class は rating から自動対応：`rA-plus-plus / rA-plus / rA / rA-minus / rB-plus-plus / rB-plus / rB`
- **評価ランクの改定**＝該当記事の `stockCard.rating` を書き換えてビルド＆デプロイするだけ

---

## 9. 検証コマンド集（2026-09-08 更新）

```bash
cd /c/Users/fuzzy/Documents/my-investment-blog

# ① 公開前チェック（必ず最初。エラーが出たら直すまでビルドしない）
py scripts/precheck.py --changed

# ② 株価を書く前に確定終値を取る
py scripts/price.py 4040 5408 --days 5

# ③ ビルド（postbuild で pagefind が走る。npx astro build は使わない）
npm run build

# ④ 壊れ太字チェック（0であること）
grep -rho '[*][*]' dist/blog/*/index.html | wc -l

# ⑤ 下書きが本番に出ていないか
for s in <draft-slug>...; do [ -d "dist/blog/$s" ] && echo "NG $s" || echo "OK $s"; done

# ⑥ デプロイ（必ず --branch=master）
npx wrangler pages deploy dist --project-name=investment-blog --branch=master

# ⑦ 本番確認（CDNキャッシュがあるので必ずキャッシュ回避）
T=$(date +%s); curl -s -H 'Cache-Control: no-cache' \
  "https://numasoko-value.com/blog/<slug>/?v=$T" | grep -c "本文中の固有フレーズ"

# ⑧ バックアップ（公開したら毎回）
py scripts/backup.py

# ⑨ 被リンクの少ない記事を探す（related から参照される回数。0や1の記事を次の related に混ぜる）
for f in src/content/blog/*.md; do s=$(basename "$f" .md); \
  echo "$(grep -h '^related:' src/content/blog/*.md | grep -o "\"$s\"" | wc -l) $s"; \
done | sort -n | head -20
```

`backup.py` は記事原稿に加えて **`_サイト設定/` 12本**（contentMap・columnAuthors・series・
BlogPost・SeriesNav・サイドバー・各ページ）と **`_スクリプト/` 4本**（precheck・price・
install_dashboard・backup 自身）もミラーする。記事だけ戻しても同じ品質で回せないため。

### タグ交差が出たときの直し方

`precheck.py` が `L211 タグ交差 <span> を </strong> で閉じている` と出したら、
**その行を構造的に修復する**（手で直すと別の場所を壊しやすい）。

```python
# scratch/fixtag.py <file> <lineno>  … 開いたタグに合わせて閉じタグを書き換える
import io, re, sys
p, lineno = sys.argv[1], int(sys.argv[2])
lines = io.open(p, encoding='utf-8').read().split('\n')
line = lines[lineno-1]; stack=[]; out=[]; pos=0; fixed=0
for m in re.finditer(r'<(/?)(span|strong)\b[^>]*>', line):
    out.append(line[pos:m.start()]); tok=m.group(0)
    if not m.group(1): stack.append(m.group(2))
    elif stack and stack[-1]!=m.group(2): tok='</%s>'%stack.pop(); fixed+=1
    elif stack: stack.pop()
    out.append(tok); pos=m.end()
out.append(line[pos:]); lines[lineno-1]=''.join(out)
io.open(p,'w',encoding='utf-8',newline='').write('\n'.join(lines))
print('L%d: %d箇所を修正'%(lineno,fixed))
```

### Windows での注意

- **絵文字や日本語を `print` すると cp932 で落ちる** → `sys.stdout.reconfigure(encoding='utf-8')` を先頭に置く
- **`python - <<'PY'` で日本語を含むスクリプトを流すと文字化けして置換が失敗する**
  → 日本語を含む置換スクリプトは**いったんファイルに書いてから実行**する
- ファイル出力は必ず `io.open(..., encoding='utf-8', newline='')`

---

## 10. ドリップ配信ワークフロー（毎日1本公開）

ユーザーは「作り貯め → 毎日1本ずつ公開」を好む。

1. 記事を書いて `draft: true` で**在庫**として保持（pubDate は公開予定日）
2. 公開当日に「本日分を公開して」と来る → `draft: true` を外す
3. **公開当日にやること（順番厳守）**
   - `py scripts/price.py <code>` で**確定終値を取り直す**（在庫の株価は古い）
   - 株価に連動する数字を全部引き直す（時価総額・EV・PBR・利回り・シナリオの騰落率・stockCard）
   - `py scripts/precheck.py <slug>` でエラー0を確認
   - `npm run build` → 壊れ太字0 → 下書きが出ていないか確認
   - `npx wrangler pages deploy dist --project-name=investment-blog --branch=master`
   - キャッシュ回避で本番確認 → `py scripts/backup.py`
   - **`git add` → `git commit` → `git push origin master`**（デプロイと git は別物。
     デプロイしただけではリポジトリに何も残らない。2026-08-24〜09-08 に16日分・61ファイルが
     未コミットのまま溜まり、新設スクリプトがローカル1コピーだけになっていた事故がある）
   - **社内掲示板（`BookSidebar.astro` の `bulletins`）に1件足す**（担当キャラの署名つき）
   - ユーザーから「Xの告知を作って」と来る（**CJK=2・URL=23の加重で280以内**。5案出すのが定番）

### 現在の在庫（2026-09-08 時点・5本）

| 公開予定 | slug | 内容 | 公開前にやること |
|---|---|---|---|
| **9/9** | `scorecard-45-stocks-2026q3` | 通信簿①（45銘柄の答え合わせ） | 基準日9/7のまま可（明記済み） |
| 9/10 | `nakayama-steel-5408` | 中山製鋼所 B+ | **株価を9/4→最新へ引き直す** |
| 9/11 | `toc-8841` | テーオーシー B+ | **株価を9/4→最新へ引き直す** |
| 9/12 | `cheap-stock-census-2026-09` | 人口調査① | **ダッシュボード更新後に数字を回し直す** |
| 9/13 | `reverse-value-02-short-screening` | 逆バリュー② | **9社の株価・PERが9/8場中値。要更新** |

### 検討中で未着手のもの

- **逆バリュー③**：イビデン（4062）と岡本硝子（7746）の戻り売り検討。
  ユーザーの見立ては「イビデンは期待先行だから中期では売れる」で、データもそれを支持。
  イビデンは7月に27,480円→12,980円（1か月で−52.8%）の実績があり、
  EPS297.2円固定でPER43.7倍＝12,980円が下値目処。売買代金1,692億円/日で逆日歩リスクが低い。
  **建てるなら23,000〜27,000円のゾーンから分割**（現値22,200円は戻りの途中で早い）。
  岡本硝子は897〜997円で第1弾（板が3.5億円/日と薄いのが難点）。
- **新規貸借銘柄の高PER株**：JPXの貸借選定27社を調べたが該当は実質ミライロ1社のみで、
  しかも売買代金400万円/日で売れない。**貸借の選定基準が流動性・規模なので、
  新規貸借＝業績が伸びた会社が集まる**という構造的な発見あり。
  「売り候補を探したら全部買い候補だった」という記事にはできる。

---

## 11. ホームのヒーロータグ ⇄ /map ジャンル連動

`src/pages/index.astro` のヒーロータグは、**キャラアイコン付きの6ジャンルタグ**で、クリックすると `/map#<theme.id>` の該当ジャンルへ飛ぶ（`map.astro` の theme-card に `id={theme.id}` と `scroll-margin-top:90px` を付与済み）。1行固定（`flex-wrap:nowrap`／モバイルのみ折返し）。

| タグ | theme.id |
|---|---|
| 🦦 資産バリュー | asset-value |
| 🦫 ニッチ独占 | moat |
| 🐊 TOB発掘 | tob-event |
| 🧓 野村流 | risk-leverage |
| 🧑‍💼 投資哲学 | philosophy |
| 🐝 高配当・増配 | income |

※ contentMap は全公開記事が分類済み（未分類なし）。野村の記事は「資金管理・レバレッジ・口座戦略（risk-leverage）＝野村流」に入っている。

---

## 12. 数字・画像の更新（サイト全体で整合を取る）

**数字を変えたら、必ず全箇所を横断で直す。** 1箇所だけ直して食い違うのが定番の事故。

| 項目 | 現在値 | 出てくる場所 |
|---|---|---|
| 買い方金利 | **1.69%** | `AssetSidebar.astro` / `company.astro` / `BookSidebar.astro` |
| 資産推移（月末） | **4,308万円**・評価損益 **+146万円** | `AssetSidebar.astro` / `company.astro` / `BookSidebar.astro`（著者bio） |
| 資産推移の画像 | `/images/nomura-asset-chart-202608.png` | 同上（月が変わったら差し替え） |
| 最新ポートフォリオ | 基準日 **2026/09/04**・172建玉 | `public/holdings.html` |
| 社内掲示板 | 最新 2026.09.06 | `BookSidebar.astro` の `bulletins`（新しい順・**公開のたびに1件追加**） |

### 最新ポートフォリオ（`/holdings`）の更新手順

1. OneDrive の `野村PF更新` で `更新.bat` を回す（→ `野村PFダッシュボード_公開用.html` が出る）
2. `py scripts/install_dashboard.py` で `public/holdings.html` に設置

**ダッシュボード上部のナビは自動生成**（2026-09-08〜）。`install_dashboard.py` の
`latest_weekly()` が `src/content/blog/weekly-*.md` の**公開済みで最新のもの**を拾うので、
週次記事を書き足せばリンクが勝手に張り替わる。実行ログに `ナビ : あり（トップ／コラム／週次 YYYY-MM-DD）` と出る。

**人口調査シリーズはこの直後に回すこと**（基準日がずれると precheck が警告する）。

---

## 13. 既知の保留・改善余地

### すぐ効く未着手

- **被リンク1本しかない記事が24本**（日本フェンオール・ヤマウHD・藤倉化成・新晃工業・
  ペンタオーシャンなど）。孤立ではないので急がないが、テーマの近い記事から1〜2本足す余地あり。
  **新規記事を足したら、その回で被リンクが0や1の記事を拾って related に混ぜると増やせる。**
  数え方は §9 ⑨ のワンライナー。

### ユーザー確認待ち

- **確定益1,137万円・源泉徴収231万円の出典**（9/6の運用記録 `weekly-2026w36-midlarge-56buys` の3箇所）。
  2026-09-08 に証券口座の CSV（譲渡益税履歴・明細969件）を取得して、ここまで判明した。

  | 出どころ | 損益 | 徴収税額 |
  |---|---|---|
  | CSVサマリー「当年」 | 11,491,691円 | 2,334,402円（税率20.313%で整合） |
  | CSVサマリー「前年」 | 7,732,645円 | 1,570,783円 |
  | 明細の暦年2026年（233件） | 3,054,844円 | — |
  | 明細の暦年2025年（625件） | 8,209,400円 | — |
  | 明細の全期間 2025/04/01〜2026/09/15（858件） | 11,264,244円 | — |

  **残る論点は「当年」の定義**。サマリーの当年（1,149万円）は暦年2026年（305万円）と一致せず、
  **CSVの期間全体（2025年4月〜）にほぼ一致する**。つまりこの口座の「当年」は暦年ではなく
  年度（2025年4月始まり）の可能性が高い。だとすると記事の「**今年は**すでに確定益が1,137万円」は
  読者に暦年として読まれるので表現が不正確。**口座画面で「当年」の期間表示を確認すること。**
  - 年度なら → 表現を「今年度（2025年4月以降）」に直し、数字を1,137万→1,149万・231万→233万へ更新
  - 暦年なら → 305万円が正しく、同じ節の「1,137万円の損失が必要」「1,137万−238万＝約899万」も書き直し
  - **CSVはリポジトリに入れない**（実取引明細）。集計値だけ使う

- 明細858件から出した**勝率63.6%（勝546／負312）、平均利益は平均損失の1.74倍**は期間集計なので、
  上の論点がどちらに転んでも有効。note の信憑性ブロックにはこちらを使っている（金額は載せていない）。

### 恒常的な注意

- 銘柄紹介の通し番号は過去分が重複。**新規は番号なし運用**で回避中。
- X告知はユーザーが投稿運用。公開直後に「Xの告知を作って」と来るのが定番。
- 記事のランク（A++〜B）は7段階。**下がりにくさ**を測る指標であって上がりやすさではない
  （通信簿①の検証で判明。±10%以上動いた5本のうち4本が下位ランクから出た）。

---

## 14. ユーザーの基本的な依頼パターン

- 一時ファイル（`C:\Users\fuzzy\Downloads\*.html` や OneDrive の `*.md`）を `@"パス"` で渡し「記事追加」。HTMLは銘柄の完全版レポート、mdはコラム下書きが多い。
- ほぼ毎回：①キャラ対話形式で面白おかしく ②見出し/タイトルはSEO意識 ③関連記事・銘柄へリンク。
- 銘柄記事では「評価ランク」（例 近況B++／長期A+）と、資産性・成長性・国策・独占などの**妙味の軸**を指定してくることが多い。
- 公開後に「Xの告知を作って」／追記要望（「○○も強調して」「競合評価を足して」）→ 該当記事に加筆して再デプロイ。
- 画像・数字（資産推移・金利）の更新依頼 → サイト内の整合（複数箇所）を取る。
```

---

## 15. 機密・公開してはいけないもの（**必ず守る**）

GitHub リポジトリ `fuzzy-citrus/investment-blog` は **PUBLIC**。

- `.cf_token`（gitignore 済み）／`portfolio-holdings.xlsx`（gitignore 済み・実ポジション）／`04_note原稿/`
  → **絶対にコミットしない**
- OneDrive の `野村保有リスト.xlsx` は実ポジション。リポジトリに入れない
- **note のパスワードを過去にスクリーンショットで共有されたことがある。
  ファイル・リポジトリ・バックアップのいずれにも書かない**
- 他AIが作った資料に **社外秘** が混ざっていたことがある
  （「社外秘：特潤の価格改定_Q&A.pdf」、出光の内部文書、南海化学の社内価格提案など）。
  **外部からもらった資料を記事や `public/analysis/` に載せる前に、
  `社外秘|機密|confidential|部外秘|取扱注意|内部資料|パスワード` で必ず grep する**
- 有料note商品は**手法を売る**。個別銘柄の推奨＋目標株価は金商法（投資助言・代理業）に触れる
- 空売り記事は特に注記を厚く（推奨でない／損失無限／踏み上げ・逆日歩／
  企業の事業を否定するものではない／ポジションの有無は述べない）

---

## 16. センシティブ題材の書き方（ユーザーの明示ルール）

- **震災・災害を扱うとき、「おいしい」「うれしい」など利得を喜ぶ表現を使わない。**
  支援が先、果実は行動で還す。
  （南海化学の南海トラフの節は、守田に「ここまで良い話が続いた。だから釣り合いを戻す」と
  言わせてトーンを切り替えている。この作りを踏襲すること）
- 🦦 河内拾造は**標準語（です/ます調）**。関西弁にしない
- 全記事にライバル乱入のボケ→ツッコミ→回収を入れる。**茶々はチェックリストに変換して回収する**
- 乱入キャラは4体（🦍テック番長／🐒レバナス小猿／🦜オルカン鸚鵡／🦅ドル建て鷹）。
  **連日で同じキャラを使わない**。直近の使用日を確認してから選ぶ

---

## 17. note 運用（2026-09-08 新設）

### アカウントと置き場所

- note ID `citrus_fuzzy_25` ／ クリエイター名「柑橘系」／ https://note.com/citrus_fuzzy_25
- **パスワードはどこにも記録しない**（パスワードマネージャ管理）。スクショでも受け取らない
- 設定メモ：`C:\Users\fuzzy\Documents\numasoko-reports-backup\ブログ運用設定.md`
- 原稿：同バックアップの `04_note原稿/`（00_プロフィール／01_低配当性向スクリーニング／02_無料_はじめまして／03_無料_優待の総合利回り）

### 役割分担（動かさないこと）

- **完全版レポートは note に移さない。** ブログ（自ドメイン）に置いたまま。移すとドメイン評価が note.com に積み上がる
- note は ①集客チャネル ②方法論・手順の有料販売 の2つに割り切る
- **有料商品に個別銘柄の目標株価・評価ランク・売買判断を入れない**（金商法の投資助言・代理業の線）

### 有料記事①（**2026-09-08 公開済み**）

| 項目 | 値 |
|---|---|
| URL | https://note.com/citrus_fuzzy_25/n/n2b2863b4d9d0 |
| タイトル | 配当利回り0.72%の株が、1株純資産を年14%増やしていた——「ROE×(1−配当性向)」で"本物の複利"だけを抜き出す方法 |
| 価格 | 500円（初商品は実績づくり優先。値上げは後からできる） |
| 本文 | 12,135字（無料2,320／有料9,801） |
| 構成 | STEP 0〜7 ／ 実データ6社 ／ 罠1〜5 ／ 効かない会社 ／ 起爆剤は無料のオプション ／ 表計算の列定義 ／ 手順チェックリスト ／ まとめ ／ 6社のブログ記事リンク |
| 有料ライン | 信憑性ブロックの直後、「STEP 0」の直前 |
| ハッシュタグ | #投資 #日本株 #バリュー株 #配当 #資産運用 #株式投資 |
| 見出し画像 | 1280×670 の自作PNG（`scratchpad/make_eyecatch.py` で生成。金 #e8b84b × 黒 #1e1e1e） |

**記事内の図（3枚）**

`scratchpad/make_figures.py` で生成し、note にアップロード済み。**再貼り付けしても消えないよう、
原稿に画像URLをマークダウンで埋め込んである**（`![...](https://assets.st-note.com/img/...)`）。
本文を組み直すときも、この記法があれば図ごと入る。

| 図 | 位置 | note のURL末尾 |
|---|---|---|
| 判別式（理論 vs 実測 → 乖離、2社の対比） | 無料・「式はこれだけです」の後 | `1788859535-Q7hq4TSKx95WB1yempAwsRdg.png` |
| 手順フロー（母集団の絞り込み） | 無料末尾・有料ラインの直前 | `1788859630-O2GKY6nbCIgM1dB9umqDUXc7.png` |
| 4パターン（A/B/C/D のカード） | 有料・STEP 4 | `1788859732-FgTaeY2hkHvUxidQlnNwXGEu.png` |

**配布物｜判別シート（Excel）**

- 生成：`scratchpad/make_sheet.py` → `public/downloads/naibu-fukuri-hanbetsu-sheet.xlsx`
- 公開URL：https://numasoko-value.com/downloads/naibu-fukuri-hanbetsu-sheet.xlsx （有料エリア内からのみ案内）
- 3シート構成：**判別シート**（空の様式40行）／**記入例**（日本ギア=A・昭和化学=B が並ぶ）／**読み方**
- **このシートは「割安かどうか」を測らない。**内部複利が本物か借り物かだけを分ける。
  記事にもシートにも明記してある。安さの評価は別の軸（東海エレクトロニクスが対照群）

**次に有料記事を作るなら**

「割安株の探し方」を別商品にする案がある。判別シートと役割が分かれていて混ざらない。

**残っている手直し**

- 「実務編｜スクリーニングの手順」が見出しにならず、直前の段落に連結している。
  一度 h2 化に成功したが、本文を組み直すと戻る（貼り付け境界で前段落に吸収されるため）。
  直すなら、貼り直し後に本文で「実務編」の直前をクリック → Enter で分割 → その段落を
  **トリプルクリックで選択 → 浮動ツールバーの「見出し」→「大見出し」**（「+」メニュー側は
  カーソル位置がずれて空見出しができる）

**ブログとの相互導線**（この記事の肝）

- note の実データ6社は**全社ともブログに記事がある**（6356／7485／4990／6748／5900／8071）。
  有料パート末尾からその6本＋`/holdings`＋トップへリンク済み
- ブログ側は `low-payout-internal-compounding` の末尾に📎ボックスで note へ送る。
  この記事が note 記事の「元になった考え方」なので、ハブはここ1本に集約する

### note へ原稿を流し込む手順（**マークダウンを直接貼らない**）

note のエディタは貼り付け時に見出しは解釈するが **`**太字**` は文字のまま残る**。
太字が100箇所以上あるので、**HTML に変換して CF_HTML 形式でクリップボードに載せる**。

```bash
# ① マークダウン → HTML（scratchpad の md2html.py。素のURLも自動リンク化する）
py md2html.py note_無料パート.md free.html
py md2html.py note_有料パート.md paid.html
```

```powershell
# ② CF_HTML でクリップボードへ（UTF-8 バイト列を MemoryStream で渡すのが肝）
Add-Type -AssemblyName System.Windows.Forms
$body = [System.IO.File]::ReadAllText($p, [System.Text.Encoding]::UTF8)
# ... ヘッダのバイトオフセットを組み立て ...
$obj.SetData([System.Windows.Forms.DataFormats]::Html, $false, $ms)   # ← $ms は MemoryStream
```

**踏んだ罠（全部実際に起きた）**

1. **CF_HTML を文字列のまま `SetData` すると日本語が全部化ける。** UTF-8 バイト列を
   `MemoryStream` で渡すこと（`$false` の autoConvert 付きオーバーロード）
2. **PowerShell 5.1 は BOMなしUTF-8 のスクリプトを ANSI として読む。** 日本語コメントが
   化けて次の行を飲み込み、変数が null になる。日本語を含む .ps1 は作らず、インラインで実行する
3. **note は本文中の `<h1>` を落とす**（h1 は記事タイトル専用）。見出しは h2 以下で作る
4. **貼り付け前にクリップボードを読み戻して確認する。** ユーザーが別のものをコピーしていて、
   証券口座の譲渡益税データが記事に貼り付いた事故がある
5. **公開設定画面で「キャンセル」を押すとタグ・価格・有料ラインが全部消える。** 本文の
   下書き保存とは別物。設定は「投稿する」まで保存されない

### プロフィール（数字は 2026-09-08 実測）

記事111本／紹介45銘柄／完全版レポート50本／平均修正PBR0.92倍・うち30銘柄が1倍割れ／保有172建玉・166銘柄。
文面は `scratchpad/note_プロフィール.md`。**旧プロフィールは「35社・40本・0.96倍・22銘柄」で全部古い。**

---
